class Solution {
    func maximumWealth(_ accounts: [[Int]]) -> Int {
        var max = 0
        var sum = 0
    for i in accounts {
        sum = 0
        for j in i {
            sum += j
        }
        if sum > max {
            max = sum
        }
    }
    return max
    }
}
