class Solution {
    func sumIndicesWithKSetBits(_ nums: [Int], _ k: Int) ->Int {
    var count = 0
    for (i,j) in nums.enumerated() {
        let binary = String(i,radix: 2).filter({$0 == "1"}).count
        if binary == k {
            count += j
        }
    }
    return count
    }

}